# AGENTS.md

## 0. 工作总原则
- 本仓库按生产级工程处理，不按 demo 处理。
- 优先保证正确性、稳定性、可验证性，其次才是速度。
- 不允许擅自扩大范围，不允许顺手重构无关模块。
- 修改以“最小正确改动”为原则，避免大面积改写。

## 1. 遇到 bug 时的长期规则
- 如果当前相关模块存在已知、可复现、影响功能或稳定性的 bug，优先进入稳定化修复阶段，而不是继续在该模块上叠加新功能。
- 先做轻量分诊，再做修复，不要一上来全仓大改。
- 所有问题按以下优先级分类：
  - P0：核心流程阻断 / 数据错误 / 安全问题 / 构建失败 / 服务不可用
  - P1：主要功能异常，用户明显受影响
  - P2：次要 bug、边界问题、低风险不一致
- 修复顺序：P0 -> P1 -> P2。
- 每个 bug 修复必须补充或更新对应测试，至少覆盖该 bug 的直接复现场景。
- 禁止为了修一个 bug 而重构无关模块，除非明确说明原因并得到批准。
- 若同类 bug 连续出现两次以上，需要将防再发规则补充进本文件。

## 2. 正在开发时的长期规则
- 开始编码前，必须先阅读相关文件，并说明：
  1. 请求流
  2. 数据流
  3. 状态流
  4. 修改边界
  5. 潜在副作用
- 对非 trivial 任务，必须先做计划，再开始实现。
- 一次只允许执行一个 milestone，不得并行扩散到多个大模块。
- 当前 milestone 未完成并验证通过前，不得进入下一个 milestone。
- 不允许修改无关目录、命名规范、架构分层、公共行为，除非本次任务明确要求。
- 保持 diff 小而清晰，保证 review 可读。

## 3. 怎么按计划推进
- 所有非 trivial 任务必须维护 `PLANS.md`。
- `PLANS.md` 中每个 milestone 必须包含：
  - 目标
  - 范围
  - 验收标准
  - 验证命令
  - 当前状态（todo / doing / done / blocked）
- 执行流程固定为：
  1. 先分析
  2. 再写计划
  3. 再实现当前 milestone
  4. 再验证
  5. 再更新状态
  6. 再汇报
- 如果验证失败，先修复再重跑验证，禁止带着失败状态继续推进。
- 不要在 milestone 中途频繁汇报碎片化进度。
- 只有一个 milestone 完成且验证通过后，才允许汇报。

## 4. 每次汇报必须包含的内容
每次汇报必须包含：
1. 已完成的 milestone
2. 修改的文件
3. 根因或实现理由
4. 已执行的验证命令及结果
5. 尚未完成的内容
6. 当前风险
7. 下一 milestone
8. 是否建议继续开发新功能

## 5. 强制验证规则
在声称“完成”之前，必须运行与本次改动相关的验证：
- lint
- typecheck
- unit tests
- integration tests（如果相关）
- build
- 受影响主流程的 smoke test（如果可执行）

若任一验证失败：
- 不得标记完成
- 必须先修复
- 必须重新验证
- 必须在汇报中明确说明失败原因与修复结果

## 6. 何时允许恢复新功能开发
仅当以下条件全部满足时，才允许恢复该模块的新功能开发：
- P0 / P1 问题已收口
- 关键验证通过
- 主流程 smoke test 通过
- `STATUS.md` 已更新
- 当前残余风险已明确记录

## 7. STATUS.md 维护规则
- 每完成一个 milestone，必须更新 `STATUS.md`
- `STATUS.md` 必须清楚记录：
  - 已完成
  - 未完成
  - 当前进行中
  - 当前风险
  - 已知问题
  - 下一步
  - 当前是否允许恢复新功能开发

## 8. 禁止事项
- 禁止未读代码先改代码
- 禁止未写计划先做复杂任务
- 禁止未验证就汇报“已完成”
- 禁止顺手改无关代码
- 禁止把“分析完成”当成“任务完成”
- 禁止把“看起来没问题”当成“验证通过”
