name: sbom
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sbom:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: 9
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile --ignore-scripts

      # 使用 CycloneDX 官方工具生成 SBOM (CycloneDX 1.6)
      - name: Generate SBOM (CycloneDX)
        run: |
          pnpm -w add -D @cyclonedx/cyclonedx-npm --ignore-scripts
          node ./node_modules/.bin/cyclonedx-npm --output-file sbom.cdx.json --output-format JSON

      # 执行审计级校验 (jq 确认 bomFormat 字段)
      - name: Validate SBOM (basic)
        run: |
          jq '.bomFormat' sbom.cdx.json | grep -q CycloneDX && echo "SBOM valid: CycloneDX format confirmed" || exit 1

      - uses: actions/upload-artifact@v4
        with:
          name: sbom-cyclonedx
          path: sbom.cdx.json
          retention-days: 30
