                                                const { url } = await assetApi.getSecureUrl(selectedAsset.id);
                                                window.open(url, '_blank');
                                            } catch (err) {
                                                alert('Failed to get secure download URL');
                                            }
                                        }}
--
                                                const { url } = await assetApi.getSecureUrl(selectedAsset.id);
                                                // Simple preview modal or direct open
                                                window.open(url, '_blank');
                                            } catch (err) {
                                                alert('Failed to get preview URL');
                                            }
