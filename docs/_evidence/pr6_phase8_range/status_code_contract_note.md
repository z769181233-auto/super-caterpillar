### 状态码契约备忘

**分歧点:**
- **API 行为**: 对于过期 (Expired) 或篡改 (Tampered) 的签名请求，返回 **HTTP 403 Forbidden** (语义正确：凭据无效)。
- **Gate 期待**: `run_launch_gates.sh` 脚本期待返回 **HTTP 404 Not Found** (历史原因或旧版架构逻辑)。

**当前策略 (Adam 裁决):**
- **本阶段 (PHASE 8)**: 暂不处理此分歧。
- **目标**: 优先打通 Range Request 206 这一产品核心链路。
- **后续**: 待 206 问题解决后，再评估是修改 API 安全逻辑以迎合门禁，还是修正门禁脚本以支持标准的 403 语义。
